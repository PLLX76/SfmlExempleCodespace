{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Main",
            "type": "shell",
            "command": "g++ -c main.cpp && g++ main.o -o sfml-app -lsfml-network -lsfml-system",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}